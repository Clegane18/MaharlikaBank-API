# Ignore the "node_modules" folder
node_modules/

# Ignore the "Config.json" file inside the "Config" folder
Config/Config.json

# Ignore some log files and temporary files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Ignore some directories used in build and coverage reports
coverage/
dist/
.next
.nuxt
.temp
.cache

# Ignore some files used by the "yarn" package manager
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.yarn-integrity

#Secret key
.env
